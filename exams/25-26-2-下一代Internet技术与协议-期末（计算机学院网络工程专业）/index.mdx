---
时间: 2025-2026学年第二学期
科目: 下一代Internet技术与协议
阶段: 期末
类型: 本科
学院:
- 计算机学院
---

## 一、单选题（每题 2 分，一共 20 分）

## 二、填空题（每空 1 分，一共 15 分）

### 1.

网络核心的两个重要功能是 <Slot /> 和 <Slot />。

<Solution>
在此录入答案或解析。
</Solution>

### 2.

NDP 检测同一链路是否存在其他节点使用相同 IP 地址的机制是 <Slot />。

<Solution>
在此录入答案或解析。
</Solution>

### 3.

组播地址的 Flag 标志中 T 为 0 表示 <Slot />。

<Solution>
在此录入答案或解析。
</Solution>

### 4.

IPv6 基本报头中 <Slot /> 字段用于标识数据流，以提供 QoS 保证。

<Solution>
在此录入答案或解析。
</Solution>

### 5.

IPv6 扩展首部中支持 IPsec 身份认证的首部类型值是 <Slot />。

<Solution>
在此录入答案或解析。
</Solution>

### 6.

UDP 基础上集成 TLS 加密、多路复用和拥塞控制的下一代传输层协议是 <Slot />。

<Solution>
在此录入答案或解析。
</Solution>

### 7.

IPv6 无状态地址自动配置（SLAAC）依赖于路由器周期性发送的 <Slot /> 信息。

<Solution>
在此录入答案或解析。
</Solution>

### 8.

TCP/IP 沙漏模型的细腰处位于 <Slot /> 层。

<Solution>
在此录入答案或解析。
</Solution>

### 9.

IPsec 2 种工作模式是 <Slot /> 和 <Slot />。

<Solution>
在此录入答案或解析。
</Solution>

### 10.

IPv6 中的 <Slot /> 字段取代 IPv4 中 TTL 字段。

<Solution>
在此录入答案或解析。
</Solution>

### 11.

为解决 IPv4 地址枯竭和路由表膨胀，Internet 采用了 <Slot /> 和 <Slot /> 技术。

<Solution>
在此录入答案或解析。
</Solution>

### 12.

IP 组播基本树类型为源树和 <Slot />，PIM-DM 主要用 <Slot />。

<Solution>
在此录入答案或解析。
</Solution>

## 三、简答题（每题 5 分，一共 25 分）

### 1.

简述全球互联网号码资源的层级分配体系。

<Solution>
在此录入答案或解析。
</Solution>

### 2.

对比 IPv6 相对 IPv4 的核心技术优势。

<Solution>
在此录入答案或解析。
</Solution>

### 3.

ICMPv6 协议整合了 IPv4 中哪些功能？列举至少 3 个主要功能或协议。

<Solution>
在此录入答案或解析。
</Solution>

### 4.

IPv6 引入扩展首部主要目的是什么？简述当路由器收到一个无法识别的扩展首部时如何处理。

<Solution>
在此录入答案或解析。
</Solution>

### 5.

简述 QUIC 协议相比传统 TCP+TLS 协议栈在连接建立（握手）中的优势。

<Solution>
在此录入答案或解析。
</Solution>

## 四、分析题（1、2 题各 10 分，第 3 题 20 分）

### 1.

IP 地址空间可使用二叉前缀树表达，请说明构建规则，并说明全球单播、链路本地、组播在前缀树中分支特点，分别计算全球单播、组播地址在整个 IPv6 地址空间的占比。

<Solution>
在此录入答案或解析。
</Solution>

### 2.

结合 IPv6 无状态地址自动配置（SLAAC）与 EUI-64 规则回答。有一主机 MAC 地址为 `00:25:96:12:34:56`，所在 IPv6 前缀为 `2001:db8:abcd::/64`。

（1）简述 EUI-64 接口 ID 生成步骤。

（2）根据规则计算主机完整的 IPv6 全球单播地址。

（3）简述主机获取 IPv6 地址完整分配流程。

<Solution>
在此录入答案或解析。
</Solution>

### 3.

简述校园网 IPv6 安全风险暴露面有哪些，设计一套园区网络 IPv6 安全风险暴露面监测系统，可以从哪几个维度设计，给出方案。

<Solution>
在此录入答案或解析。
</Solution>
