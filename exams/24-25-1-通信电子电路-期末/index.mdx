---
时间: 2024-2025学年第一学期
科目: 通信电子电路
类型: 本科
阶段: 期末
答案完成度: 残缺
学院:
- 信息与通信工程学院
来源: 565f891227971538959c85ae716caa11
---

## 一、选择题（请选出最恰当的一个答案）（每空2分，共20分）

1. 在通信系统中，信号的解调过程发生在哪个设备 <Slot />。

<Choices>
    - 发送设备
    - 输入变换器
    - 接收设备
    - 信源
</Choices>

2. 如果有用信号为弱信号，干扰信号为强信号，输出信号中包含干扰信号的调幅信息，这种现象称为 <Slot />。

<Choices>
    - 交叉调制
    - 互相调制
    - 增益压缩
    - 阻塞
</Choices>

3. 在射频和微波频段中最常用来描述双端口网络的等效参数是 <Slot />。

<Choices>
    - Y 参数
    - Z 参数
    - H 参数
    - S 参数
</Choices>

4. 在使用单谐振电路作为负载的放大电路中，品质因数越大，<Slot />。

<Choices>
    - $3\,dB$ 带宽越大
    - $3\,dB$ 带宽越小
    - 矩形系数越大
    - 矩形系数越接近于 $1$
</Choices>

5. 以下各频率源中稳定度最高的是 <Slot />。

<Choices>
    - 标称频率 $1\,\mathrm{MHz}$，实际频率 $998\,\mathrm{kHz}$
    - 标称频率 $100\,\mathrm{MHz}$，实际频率 $99.8\,\mathrm{MHz}$
    - 标称频率 $1\,\mathrm{GHz}$，实际频率 $1.002\,\mathrm{GHz}$
    - 标称频率 $2\,\mathrm{GHz}$，实际频率 $2.002\,\mathrm{GHz}$
</Choices>

6. 设接收机收到的信号频率为 $931\,\mathrm{kHz}$，中频为 $465\,\mathrm{kHz}$，经过混频和检波之后产生了频率为 $1\,\mathrm{kHz}$ 的嘨叫，则该嘨叫属于 <Slot />。

<Choices>
    - 组合频率干扰
    - 中频干扰
    - 镜像干扰
    - 组合副波道干扰
</Choices>

<Figure src="题图1.7.svg" float>
    第1.7题图
</Figure>

7. 图1-7所示为正交振幅调制（QAM）调制端的方框图。若1路信号的基带带宽为 $1\,\mathrm{MHz}$，Q路信号的基带带宽为 $1.5\,\mathrm{MHz}$，则输出信号 $v_o(t)$ 的带宽为 <Slot />。

<Choices>
    - $1\,\mathrm{MHz}$
    - $2\,\mathrm{MHz}$
    - $2.5\,\mathrm{MHz}$
    - $3\,\mathrm{MHz}$
</Choices>

<Figure src="题图1.8.svg" float>
    第1.8题图
</Figure>

8. 图1-8所示 ACG 电路框图中，低通滤波器最主要的作用是 <Slot />。

<Choices>
    - 避免出现反调制现象
    - 提高环路增益
    - 减小静态误差
    - 降低接收机噪声总系数
</Choices>

9. 现欲设计一变容二极管直接调频电路，在以下不同电容变化指数 $\gamma$ 的变容二极管中，选择哪一个最为合适？<Slot />

<Choices>
    - $\gamma=1$
    - $\gamma=2$
    - $\gamma=3$
    - $\gamma=4$
</Choices>

10. 以下不属于射频接收机指标的是 <Slot />。

<Choices>
    - 灵敏度
    - 动态范围
    - 选择性
    - 临道泄漏比
</Choices>

## 二、判断题（每空1分，共10分）

1. <Blank /> 信道是通信系统中传输信号的媒介，可以是无线的也可以是有线的。

2. <Blank /> 任何电通信系统中都存在着噪声，但可以通过技术手段降低其对有用信号的影响。

3. <Blank /> 噪声系数这一概念可被用来描述线性电路和非线性电路。

4. <Blank /> 由于小信号谐振放大电路中放大器的输入信号一般都比较小，因此放大器工作在甲类状态，可以采用微变等效电路法来分析。

5. <Blank /> 甲类、乙类、丙类电路的区分依据是放大器件在输入信号的一个周期内是否会进入截止区以及进入该区域的时长。

6. <Blank /> 推导可知，三阶互调截点的电平（$V_\text{HP3}$）要比 $1\,\mathrm{dB}$ 压缩点的电平（$V_\text{im-1dB}$）高 $10\,\mathrm{dB}$ 左右。

7. <Blank /> 当能够正常起振时，石英晶体振荡器的振荡频率基本上由石英晶体的工作频率所决定因此选频网络的 $L$, $C$ 参数可以任意选择。

8. <Blank /> 在甲类起振丙类工作的自偏振荡电路中，当电路的工作点进入丙类时环路增益立即变为 $1$，从而满足巴克豪森准则，达到平衡。

9. <Blank /> VCO 在锁相环中可以起到积分器的作用。

10. <Blank /> 功率回退是指在设计发射机中功率放大器的电路和选择器件时，通过指标分解，使得功率放大器的输入信号平均功率比其 $1\,\mathrm{dB}$ 压缩点低 $6-10\,\mathrm{dB}$。

## 三（5分）

一个阻值为 $2\,\mathrm{M\Omega}$ 的电阻，其工作温度为 $170\,\mathrm{^\circ C}$，工作带宽为 $5\,\mathrm{MHz}$，试计算该电阻两端噪声电压的有效值和该电阻的额定噪声功率（设玻尔兹曼常数 $k=1.38\times10^{-23}\mathrm{J/K}$）

## 四（4分）

设一阶锁相环中正弦鉴相器的鉴相灵敏度 $A_d=3\,\mathrm{V/rad}$，VC0 的压控灵敏度和自由振荡角频率分别为 $A_0=2\pi\times2\times10^4\,\mathrm{rad/(s\cdot V)}$，$\omega_r=2\pi\times1.5\times10^6\,\mathrm{rad/s}$，且设 VC0 的控制范围足够大。问：

1. 当输入信号和频率 $\omega_1=2\pi\times1.53\times10^6\,\mathrm{rad/s}$ 时，环路能否进入锁定？
2. 若能锁定，稳态相位误差是多少？此时控制电压多大？
3. 若不能锁定，请给出你对电路的修改方案，以使得电路能够 进入锁定状态。

**注意：请根据你在第 (1) 问的回答，从 (2), (3) 两问择一回答即可。**

## 五（12分）

<Figure src="题图5.svg" float>
    第5题图
</Figure>

如何减小负载电阻及信号源内阻对谐振回路的影响，是设计选频放大器时必须要考虑的问题之一。部分接入电路可以通过改变电路的连接方式来提高品质因数，而无需改变信号源内阻和负载电阻，得到了广泛应用。设如图5所示的部分接入电路处于谐振状态，其信号源内阻 $R_s0=80\,\mathrm{k\Omega}$, $i_s=\cos(2\pi\times1.59\times10^6t)\,\mathrm{mA}$，负载电阻 $R_L=20\,\mathrm{k\Omega}$，若要求回路 $Q$ 值为 $50$，且不计电感 $L_1$, $L_2$ 的铜损电阻和互感，并要求 $L_1=L_2$，试：

1. 指出电流源的频率是多少 $\mathrm{Hz}$。
2. 计算该电路的部分接入系数。
3. 计算将电路等效为并联谐振回路后，回路中并联的总电阻值和回路的特征阻抗值？
4. 为满足设计要求，请给出电感 $L_1$, $L_2$ 的电容 $C$ 的取值。
5. 计算该电路的 $3\,\mathrm{dB}$ 带宽。

## 六（14分）

某谐振功率放大电路工作在临界状态，已知其电源电压 $V_{CC}=25\,\mathrm V$，集电极电压利用系数 $\xi=0.8$，回路谐振阻抗 $R=50\,\Omega$，放大电路的集电极效率 $\eta=80\%$。试求：

1. 负载上的基波电压振幅 $C_{c1m}$、电流振幅 $I_{c1m}$、输出功率 $P_O$。
2. 集电极直流电源功率 $P_{DC}$、电源提供的直流电流 $I_{C0}$。
3. 集电极功耗 $P_C$。
4. 若由于某种原因导致回路失谐，此时晶体管集电极功耗将如何变化，为什么？

## 七（12分）

<Figure src="题图7.svg" float>
    第7题图
</Figure>

作为一种模拟调制技术，幅度调制具有悠久的历史。虽然当前的主流是数字通信，但幅度调制在航空无线电通信中仍有应用。已知某调幅波的表达式为 
$$
v(t)=2\cos(2\pi\times10^5\times t)+0.6\left[\cos(2\pi\times9\times10^4\times t)+\cos(2\pi\times11\times10^4\times t\right] \,\mathrm{V}
$$
试：

1. 说明这是哪种类型的调幅波，说明其载波频率 $f_0\;(\mathrm{Hz})$，调制信号频率 $f(\omega)\;\mathrm{Hz}$，计算其调幅系数 $m_a=$？
2. 现欲采用图中所示大信号峰值包络电路对该信号进行检波，请说明该电路时间常数的选择范围。
3. 若某人在实验室中搭建该电路时不慎将二极管接反，电路还能工作吗，其输出波形会有什么变化，为什么？

## 八（8分）

<Figure src="题图8.svg" float>
    第8题图
</Figure>

三端振荡电路是一种常用的反馈式正弦波振荡电路。为了满足巴克豪森准则，在电路稳定振荡电，三端振荡电路中的谐振网络具有反相作用。图中所示为一个三端振荡电路的交流通路，试分析以下两种情况是否能振荡？如果能振荡的话，振荡频率与各回路的谐振频率（即 $f_{01},\,f_{02},\,f_{03}$）有何关系？

1. $L_1C_1>L_2C_2>L_3C_3$；
2. $L_1C_1<L_2C_2<L_3C_3$；

## 九（15分）

<Figure src="题图9.svg" float>
    第9题图
</Figure>

在设计无线通信接收机时，由于各种因素的影响，接收机接收到的信号强弱变化范围很大，此时需要使用到自动增益控制。图中所示为某超外差式无线接收机的一部分。其中低噪声放大器的功率增益为 $30\,\mathrm{dB}$，两个无源带通滤波器的插入损耗以及无源混频器的衰减均为 $3\,\mathrm{dB}$，电路输入信号功率 $P_{si}$ 的范围为 $-100\,\mathrm{dBm}\sim-40\,\mathrm{dBm}$，且该接收机各模块均已实现阻抗匹配，试：

1. 若要求输入至 ADC 的信号功率 $P_{so}$ 固定为 $16\,\mathrm{dBm}$，求中频放大器功率增益 $A$ 的取值范围。
2. 若系统特性阻抗为 $50\,\Omega$，求 $16\,\mathrm{dBm}$ 所对应单频正弦波的振幅。
3. 若进一步知道低噪声放大器的噪声系数为 $3\,\mathrm{dB}$，中频放大器的噪声系数为 $10\,\mathrm{dB}$，求该链路从低噪声放大器的输入端到 ADC 输入端的噪声系数 $NF_{total}$。（设温度为 $290\,\mathrm{K}$）
4. 若该系统的带宽是 $10\,\mathrm{MHz}$，试求当电路输入有用信号功率为 $-90\,\mathrm{dBm}$ 时，ADC 输入端的信噪比。
