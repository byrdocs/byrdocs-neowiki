---
时间: 2025-2026学年第一学期
科目: 网络空间安全导论
阶段: 期中
类型: 本科
学院:
- 网络空间安全学院
答案完成度: 完整可靠
---

## 一、判断题（每题2分，共20分）

1. 客观上讲，网络空间安全的根本原因是网络空间存在着漏洞或缺陷，且还存在网络攻击。<Blank>√</Blank>
2. 密码算法是公开的，保证密文信息不被破解关键就在于所使用密钥的安全。<Blank>√</Blank>
3. 公钥密码算法出现以后，对称密码算法在实际应用中已无使用价值了。<Blank>×</Blank>
4. 在 RSA 算法中，$n$ 是公开的，且 $n=p\times q$（其中 $p$ 和 $q$ 都是素数)，所以，无论 $n$ 为多大，$p$ 或 $q$ 都是很容易推算出来的。<Blank>×</Blank>
5. 哈希函数的安全性是指根据已知的哈希值不能推出对应的消息原文。<Blank>×</Blank>
6. 在数字签名方案中，消息签名值的长度与签名消息的长度无关。<Blank>√</Blank>
7. 恶意软件是指未经用户允许的情况下，在用户计算机中安装的软件，所以，用户计算机中通常不会有恶意软件的。<Blank>×</Blank>
8. 从防火墙发展来看，防火墙不仅要求具有很强的边界访问控制能力，而且还要求具有强大审计和自动分析能力。<Blank>√</Blank>
10. 目前，网络攻击的目的就是获取经济利益。<Blank>×</Blank>

## 二、选择题（每题2分，共10分）

1. 按现在的计算能力，RSA 的密钥长度至少为 <Slot /> 才是安全的。

<Choices>
    - 128位
    - 256位
    - 1024位
    + 2048位
</Choices>

2. 数字证书一般要包含很多重要信息，下列哪个信息是不可能在数字证书中存在的。 <Slot />

<Choices>
    - 持有人的公钥
    + 持有人的私钥
    - 证书机构标识
    - 证书持有人姓名
</Choices>

3. 设 hash 函数输出的哈希值长度为 $n$ 比特，则安全的 hash 函数寻找碰撞的复杂度应该为 <Slot />。

<Choices>
    - O(n)
    - O(2n)
    - O(2^n)
    + O(2^(n/2))
</Choices>

4. 2003 年出现的“冲击波”病毒给全球互联网带来的直接损失将在几十亿美元左右，这种病毒传染方式是利用 <Slot />。

<Choices>
    + 漏洞
    - 文件下载
    - 盗版软件
    - 电子邮件
</Choices>

5. 网络攻击通常是有明确的目标的，DDos 攻击是一种针对系统 <Slot /> 的攻击。

<Choices>
    - 机密性
    - 完整性
    + 可用性
    - 不可否认性
</Choices>

## 三、填空题（每空2分，共16分）

1. 网络空间安全层次模型分为 4 个层次：物理安全、<Blank>系统安全</Blank>、<Blank>数据安全</Blank>、应用安全。
2. 消息认证码的功能是指 <Blank>验证信息的来源是真实的</Blank>、<Blank>验证消息的完整性</Blank>。
3. 在一个计算机网络中，防火墙的主要功能是访问控制，入侵检测的主要功能作用是 <Blank>识别正在攻击</Blank>，漏洞扫描的作用是 <Blank>报告系统潜在危险（弱点、漏洞）</Blank>。
4. 木马攻击的主要实现技术有 <Blank>植入技术</Blank>、自动启动技术、隐蔽技术、<Blank>远程监控技术</Blank>。

## 四、术语解释（共14分）

### 1. 认证性（4分）

<Solution>
确保一个消息的来源或消息本身被正确地标识，同时确保该标识没有被伪造，分为实体认证和消息认证。
</Solution>

### 2. 防火墙（4分）

<Solution>
一种高级访问控制设备，置于不同网络安全域之间，它通过相关的安全策略来控制（允许、拒绝、记录）进出网络的访问行为。
</Solution>

### 3. 数字签名（6分）

<Solution>
所谓数字签名（Digital Signature），也称电子签名，是指附加在数据单元上的一些数据，这些数据是利用签名算法首先对该数据单元进行关键信息提取，然后使用签名者的独有信息进行签名而形成的，用于标识签名者的身份以及签名者对该数据单元的认可，并能被接收者利用签名者的公有信息来验证该数据单元是否被篡改、伪造或抵赖。
</Solution>

## 五、简答题（共24分）

### 1. 公钥密码的基本思想及为什么能解决密钥分发

<Solution>
1. 接受方拥有公私钥对，其中其公钥公开，发送方可获得接受方的公钥。
2. 发送方采用公钥加密算法以接受方的公钥作为加密密钥对发送方发送的信息加密得密文。
3. 发送方通过不安全信道将密文发送给接受方。
4. 接受方收到密文后使用自己的私钥对密文解密还原出发送方发送的信息。

公钥密码体制能解决密钥分发问题：

1. 传输的安全：由于使用接收方的公钥加密，攻击者即使得到密文也无法获得发送方发送的信息。
2. 只有接收方才能得到发送方发送的信息。
</Solution>

### 2. Hash 函数的性质（包括特点和安全性）

<Solution>
输入任意长消息，输出固定长度的哈希值；

容易计算：对任何给定的消息，容易计算其哈希值。

单向性：对于给定的哈希值 $h$，要找到 $M$ 使得 $H(M)=h$ 在实际上是不可行的。

抗弱碰撞性：对于给定的消息 $M_1$，要发现另一个消息 $M_2$ 满足 $H(M_1)=H(M_2)$ 在计算上是不可行的。

抗强碰撞性：找任意一对不同的消息 $M_1, M_2$ 使 $H(M_1)=H(M_2)$ 在计算上是不可行的。

雪崩效应：当输入消息有一点变化时，输出的哈希值变化很大。
</Solution>

### 3. 计算机病毒的简评

<Solution>
网络化：计算机网络（互联网、物联网、通信网）成为计算机病毒的主要传播环境。

多样化：计算机病毒的传播途径多样化。

智能化：计算机病毒衍生速度快、寄生方式多、变化大。

黑客化：计算机病毒技术与黑客技术日益融合。

功利化：物质利益或特殊目的成为推动计算机病毒发展的最大动力。
</Solution>

### 4. 以 TCP 三次握手为例描述 DoS 攻击基本思想

<Solution>
1. 客户端（攻击者）向它希望与之通信的服务器发送请求数据包，其中源地址是伪造的。
2. 服务器能够接受请求，并按照伪造的源地址进行响应。
3. 服务器等待客户端的回应，由于源地址是伪造的，服务器等不到回应，从而消耗资源。
</Solution>

## 六、灵活题（共16分）

### 1. 通过这门课的学习，谈谈你的收获和期望。（8分）

### 2. 评价一下这门课程（包括助教）。（8分）
