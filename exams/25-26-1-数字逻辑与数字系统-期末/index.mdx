---
时间: 2025-2026学年第一学期
科目: 数字逻辑与数字系统
阶段: 期末
类型: 本科
学院:
- 计算机学院
答案完成度: 残缺
---

## ?????

从输出来看时序电路可以分为哪两类，有什么特点？

## ????

时序电路设计：

<Figure src="题图????.svg" float />

$$
\begin{cases}
    Q_1^{n+1}=A(Q_1^n+Q_0^n)\\
    Q_0^{n+1}=\overline{Q_1^n}A\\
    T=AQ_1^n\overline{Q_0^n}
\end{cases}
$$

1. 写次态方程、输出方程
2. 画状态图
3. 说明功能

## ???

<Figure src="题图???.svg" float />

A,B,C 至少同时摁下两个时灯会亮（摁下为 1，亮为 1）

1. 画真值表
2. 写最简函数式
3. 用双四选一实现

## ??

<Figure src="题图??.svg" float />

1. 画 ASM 图，状态从 `S0` 到 `Sn`。
2. 写出全部控制信号表达式。
3. 写出触发器次态方程、激励函数。

## ?

用 Verilog 设计 20 进制计数器：

<Figure src="题图?.svg" float />

（输入 `clr`, `clk`，输出 `qout` 和 `co`）

1. 要求 `clr` 异步清零，低有效。
2. 要求计数达最大时输出进位信号 `co`。
