---
时间: 2024-2025学年第一学期
阶段: 期末
科目: 离散数学
类型: 本科
来源: 5bffaf4ebcf2633173d9f47eab493b6a
答案完成度: 残缺
学院:
- 电子工程学院
---

## 一、选择题（每题2分，共10分）

1. $(\{\varnothing,\{\varnothing\}\}\oplus\{\{\varnothing\}\})-\varnothing=$ <Slot />，$\oplus$ 为集合的对称差运算，$-$ 为集合的差运算。

<Choices>
    - $\varnothing$
    - $\{\varnothing\}$
    - $\{\varnothing,\{\varnothing\}\}$
    - $\{\{\varnothing\}\}$
</Choices>

2. 设 $R(x)$, $x$ 是实数，$Q(x)$，$x$ 是有理数，$D$ 为全总个体域，则命题“并非每个实数都是有理数”的符号化形式为 <Slot />。

<Choices>
    - $\lnot\forall x(R(x)\rightarrow Q(x))$
    - $\forall x(R(x)\land Q(x))$
    - $\exists x(R(x)\land Q(x))$
    - $\forall x(R(x)\rightarrow Q(x))$
</Choices>

3. 基于 26 个英文字母的字符串拼接运算构成的代数系统，其幺元是 <Slot />。

<Choices>
    - 全串
    - 当前字符串的补串
    - 空串
    - 当前字符串的逆串
</Choices>

4. 设 $f(x)=(x+1)^2$, $g(x)=\log{x}$，则 $f\circ g$ 为 <Slot />。

<Choices>
    - $\log(x+1)^2$
    - $(\log(x+1))^2$
    - $(\log x)^2+1$
    - $(\log x+1)^2$
</Choices>

5. 设函数 $f(x)=\begin{cases}x,&x\ge0\\-x^2+1,&x<0\end{cases}$ 与 $g(x)=\begin{cases}2x+1,&x>0\\5x-1,&x<0\end{cases}$，则函数 $f$ 和 $g$ 的类型分别是 <Slot />。

<Choices>
    - 满射，单射
    - 双射，双射
    - 双射，满射
    - 满射，双射
</Choices>

## 二、填空题（每空2分，共28分）

1. 命题公式 $\lnot(p\rightarrow q)\land q$ 的类型是 <Blank />。

2. 命题公式 $p\land q$ 仅用联结词 $\{\lnot,\lor\}$ 可表示为 <Blank />，仅用联结词 $\{\uparrow\}$ 可表示为 <Blank />，仅用联结词 $\{\downarrow\}$ 可表示为 <Blank />。

3. 设 $\mathbf{A}=\{p,q,r\}$，则 $|P(\mathbf{A})|$，由 $\mathbf{A}$ 中命题变项所构造的命题公式共有 <Blank /> 种真值组合。

4. 设 $\mathbf{A}=\{a,b,c,d\}$ 二元关系 $\mathbf{R}=\{<a,b>,<c,d>,<a,c>,<c,a>\}$，则 $\mathbf{R}$ 的对称闭包 $s(\mathbf{R})=$ <Blank />。

5. 设 $\mathbf{Z}$ 为整数集合，$+$ 为普通加法，则代数系统 $<\mathbf{Z},+>$ 上的运算 $2^0=$ <Blank />, $2^{10}=$ <Blank />, $2^{-10}=$ <Blank />。

6. 群 $<\mathbf{Z}_9,\oplus>$ 中，$\mathbf{Z}_8=\{0,1,2,3,4,5,6,7,8\}$，对 $\forall x,y\in\mathbf Z_9$，有 $x\oplus y=(x+y)\mod9$，则 $2$ 的阶为 <Blank />，$3$ 的阶为 <Blank />。

<Figure src="题图2.7.svg" float>
    题图2.7
</Figure>

7. 图 $G=<V,E>$ 如下图所示，则顶点 $v_3$ 的度数是 <Blank />，其点割集为 <Blank />。

## 三、简答题（每题5分，共20分）

### 1.

在命题逻辑中，试把原子命题表示为 $p,q,r$，将下列命题符号化：

1. 他已完成考试并且买到了车票，他才能返回家乡；
2. 她被要求集中或居家办公 1 周。

### 2.

在一阶逻辑中，设个体域为全总个体域，将下列命题符号化：

1. 兔子比乌龟跑得快；
2. 有一数比任何数都大。

### 3.

设 $\mathbf Z$ 为整数集合，$k\in\mathbf Z$，二元关系 $\mathbf R=\{<x,y>|x-y=kt,\forall x,\forall y,\forall t\in\mathbf Z\}$，证明 $\mathbf R$ 为等价关系。

### 4.

求生成元为 $a$ 的 15 阶循环群的所有生成元和所有子群。

## 四、计算题（42分）

### 1.

分别使用真值表法、等值演算法和主析取范式法判断 $((p\lor q)\land(p\rightarrow q))\leftrightarrow(q\rightarrow p)$ 的类型。

### 2.

设 $A=\{\phi,\{a,b,c,d,e\},\{b,c\},\{a,b,c,d\},\{c,d\},\{b,c,d,e\},\{c\}\}$，$R$ 为包含关系（设 $<x,y>\in R$，则有 $x\subset y$），在偏序集 $<A,R>$ 中分别求：

1. $<A,R>$ 的哈斯图；
2. 设 $B=\{\{c,\},\{b,c\}\}$，求 $B$ 的上界、下界、最小上界和最大下界；
3. 设 $C=\{\{a,b,c,d\},\{b,c,d,e\},\{b,c\},\{c,d\}\}$，求 $C$ 的极大元、极小元、最大元和最小元；
4. $<A,R>$ 能否构成格，为什么？

### 3.

某工厂有甲类型元件 40 件，乙类型元件 20 件，丙类型元件 10 件，安装上 3 种元件至少一种的产品总数为 55 个，其中有 5 个产品安装了全部 3 种元件，设 A, B, C 分表示甲、乙、丙类型元件集合，试运用包含排斥原理求安装了 2 种元件的产品个数。

### 4.

使用构造证明法证明：

前提：$\lnot(p\rightarrow q)\rightarrow\lnot(r\lor s),\;(q\rightarrow p)\lor\lnot r,\;r$

结论：$p\leftrightarrow q$

### 5.

<Figure src="题图3.5.svg" float>
    第3.5题图
</Figure>

有向图 $D$ 如下图所示，求：

1. 该图的邻接矩阵 $A$；
2. 通过矩阵运算求出 $D$ 中各顶点间长度为 $3$ 的通路数，使用矩阵表示；
3. 通过矩阵运算求出 $D$ 中各顶点间长度小于等于 $2$ 的通路数，使用矩阵表示。
