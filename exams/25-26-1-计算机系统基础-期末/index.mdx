---
时间: 2025-2026学年第一学期
科目: 计算机系统基础
阶段: 期末
类型: 本科
学院:
- 计算机学院
答案完成度: 残缺
---

## 一、单选题

1. x86-64中，`long`类型的Tmin是多少？
2. x86-64中，有一单精度浮点数十六进制表示为 `0x????`，它的十进制值是？

## 二、判断题

设 `x` 为整型，`ux` 为无符号整型，`f` 为浮点型且不为 `NaN`。对的画圈，错的打叉。

1. 若 `x <= 0`，则 `-x >= 0`。<Blank />
2. 一定存在 `-(-f) == f`。<Blank />
3. （题目缺失）<Blank />

## 三、大题

### 1. 链接与库

有两可重定位目标文件 `main1.o` 和 `main2.o`，他们分别依赖相应的静态库文件：

- `main1.o` 依赖 `libx.a`
- `main2.o` 依赖 `liby.a`, `libz.a`
- `libz.a` 依赖 `liby.a`

1. `gcc -static -o main1 main.o ./libx.a` 命令会不会链接出错？如果会，分析原因并给出不会报错的正确写法。
2. 写出生成可执行文件 `main2` 的 gcc 命令。

### 2. 数值运算与标志位

给出的数均为 **9位长** 的数，有的是有符号数，有的是无符号数。填写表格中空缺的部分：

| 变量 | 二进制位表示 | 十进制表示 | 数据类型 | CF | OF | SF |
| --- | --- | --- | --- | --- | --- | --- |
| x | 110010000 | （） | 有符号数 | - | - | - |
| y | 110010000 | （） | 无符号数 | - | - | - |
| x-y | （） | （） | （） | （） | （） | （） |
| x+y | （） | （） | （） | （） | （） | （） |

### 3. 汇编优化

处理器要运算 `X * 20` 的结果，X 是 `long` 类型，存储在 `%rax` 中，回答下列问题：

1. 简单写出暴力算法（可使用乘法操作符）。
2. 用移位的优化方式写出优化后的汇编代码（本问及次问可使用 `%rdx` 作临时变量）。
3. 再用一种优化方式写出一种优化后的汇编代码。

### 4. 除法优化

用汇编语言或C语言，写出 `x / 32` 通过移位操作完成的方法。

### 5. 执行追踪

给定一连串的地址和寄存器，依次运行汇编语言命令后，结果如何？（题目具体数据缺失）

### 6. 函数调用与地址

一个 main 函数，它需要通过传入 array 数组的地址和 sum 函数的地址（分别使用绝对地址和相对引用），并分析 sum 函数执行后的返回值是多少。

sum 的 C 语言表述为：

```c
int i, result;
for (i = 0, result = 0; i < 2; i++) {
    result += array[i];
}
return result;
```

### 7. Switch 语句逆向

一个基于 switch 语句的 C 函数，它的 C 程序残缺，编译结果已知，通过汇编语言对应补全原 C 函数（给定了跳转表的地址及内容）。

### 8. 栈保护与缓冲区溢出

一个函数，它的 C 语言表述为：

```c
int i;
int arr[5] = {0};
for (i = 0; i < 5; i++) {
    arr[i] = 0;
    printf("Hello World");
}
gets(arr);
```

同时提供了对应的汇编代码（未给出具体代码，仅描述）。

1. 分析某奇怪的两句汇编语句的用途。分析另外两句奇怪的汇编语句的用途。它们组合起来有什么作用？（分别是用于设置金丝雀值/Canary 和检验金丝雀值是否被改变）
2. 分析 `i` 存在栈的哪个位置？（以 `rbp + 偏移量` 的方式表示）
3. 分析 `arr[3]` 的位置在哪里？（要求同上）
4. 输入一段字母，请问输入多少会使函数出错？

### 9. UNIX I/O 与 进程控制

有两文件 `t1.txt` 和 `t2.txt`，分别有 “BUPTCT” 和 “CMUMIT” 内容。

函数设置一个 `buf` 字符型数组，容量为 6。创建三个文件描述符 `fd1`，`fd2`，`fd3`，分别指向 `t1.txt`, `t2.txt`, `t2.txt`。

过程如下：

- 先读 `fd1` 中几个字符到 `buf`
- 再读 `fd3` 几个字符到 `buf`
- `dup2(fd3, fd1)`
- 之后 `fork`
  - 若 fork 返回 0，则再从 `fd2` 读几个字符到 `buf`
  - 若 fork 返回非 0，则从 `fd1` 读几个字符到 `buf`
- 分别在 `dup2` 前，`fork == 0` 分支，`fork != 0` 分支打印 `buf` 内容。

问题：

1. 写出 `buf` 的三次内容。
2. 每次进程启动时，都自动打开（ ）、（ ）、（ ）三个文件。
3. 执行 fork 时，从（ ）态转到（ ）态（两空均填系统状态）。

### 10. 标准I/O优缺点

写出标准I/O的优点和缺点（相比于UNIX I/O）
