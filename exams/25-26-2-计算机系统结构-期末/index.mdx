---
时间: 2025-2026学年第二学期
科目: 计算机系统结构
阶段: 期末
类型: 本科
学院:
- 计算机学院
---

## 选择题（25题, 每题1分）

## 填空题（20空，每空1分）

1. 冯·诺伊曼结构计算机包括输入设备，<Blank />、<Blank />、<Blank />、输出设备。
2. 定量设计原理包括<Blank />、<Blank />、<Blank />和CPU性能公式。
3. 两个或两个以上的事件在同一时刻发生的是<Blank />，两个或两个以上的事件在同一时间间隔发生的是<Blank />。
4. 用软件的方法在一台现有的机器上实现另一台机器的指令集的是<Blank />，用一台现有机器上的微程序去解释实现另一台指令集的是<Blank />。
5. <Blank />流水线的输出端任务流出的顺序与输入端任务流入的顺序完全相同。
6. 交换函数$C_k, N= \log_2 (n)$，取反的是二进制编号的<Blank />位。

## 计算题

### 1. (6分)
MUL指令的速率提升了20倍，总程序速度提升了2倍，求MUL指令提升前在整个程序中的时间占比。若MUL指令效率可以提升无穷倍，整个程序的加速比能到多少。

### 2. (6分)
CPU时钟2.5GHz，一段程序总指令数为1,200,000，各类型指令详情：

| 占比 | CPI |
| --- | --- |
| 40% | 1 |
| 35% | 2 |
| 25% | 5 |

求平均CPI，程序总执行时间和MIPS。

### 3.(6分)
在CRAY-1上进行向量运算，假设把向量数据元素送往向量功能部件以及把结果存入向量寄存器，从存储器中把数据送入访存功能部件需要二拍时间，程序如下，向量长20，求最快执行完程序需要的拍数。

- $V3 \leftarrow$ 存储器 8拍
- $V2 \leftarrow V0 + V1$ 8拍
- $V4 \leftarrow V2 \times V3$ 12拍


### 4.（6分）
流水线分为IF、ID、EX、MEM、WB五段，其中除EX外每段一拍，EX执行段ADD和SUB需一拍，MUL需3拍，DIV需2拍。时钟频率2GHz，程序如下：

```
ADD F1, F2, F3
SUB F3, F1, F2
MUL F4, F1, F5
DIV F6, F3, F4
```
1. 程序中的真数据相关有几个，列出
2. 使用定向传送技术，画出流水线时空图，计算程序所需时间

## 简答题

### 1.(6分)
为什么Tomasulo算法可以解决WAW冲突和WAR冲突，但是无法解决RAW冲突？

### 2.(6分)
给出五维超立方体中(0,1,1,0,0)到(1,1,0,1,0)的最短路径。

### 3.(6分)
说明监听式协议写作废策略在下列情况处理机会如何响应：
- 处于S状态，本机发生CPU写命中
- 处于S状态，收到其他机器的WtMiss信号

### 4.(13分)
题目大意：考查Tomasulo算法，填指令状态表，保留站，寄存器状态表三个表格。

